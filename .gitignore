/out
/datagen
/extras
/build-shell
/.sccache
/.cargo-home

# Local-dev plugin path overrides consumed by isabelle-core gen_shell.py.
# Never committed — they point at developer-local checkouts.
flavours/*.dbg
